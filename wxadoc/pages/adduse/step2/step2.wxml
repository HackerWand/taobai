<view class="add-block">
  <view class="step">
    <text class="add-title">这笔消费为的谁？</text>
    <scroll-view scroll-y="true" class="item-list">
      <button bindtap="next" data-item="{{ item }}" wx:for="{{ step_node }}" wx:key="index" wx:for-item="item">{{ item.name }}</button>
    </scroll-view>
  </view>
</view>